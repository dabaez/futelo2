// Runs before every test file.
// Imports @testing-library/jest-dom so matchers like toBeInTheDocument(),
// toBeDisabled(), toHaveTextContent() etc. are available globally.
import '@testing-library/jest-dom';
